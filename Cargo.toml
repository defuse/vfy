[package]
name = "backup-verify"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
blake3 = "1"
ctrlc = "3"
rand = "0.8"

[dev-dependencies]
assert_cmd = "2"
predicates = "3"
